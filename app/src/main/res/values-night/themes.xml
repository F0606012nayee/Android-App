<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyEventNote" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/black</item>
        <item name="actionMenuTextAppearance">@style/Theme.MyEventNote.Menu</item>
        <item name="materialAlertDialogTheme">@style/Theme.MyEventNote.AlertDialog</item>
    </style>

    <style name="Theme.MyEventNote.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MyEventNote.Menu" parent="TextAppearance.AppCompat.Menu">
        <item name="android:textSize">20sp</item>
    </style>

    <style name="Theme.MyEventNote.AlertDialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert"/>
</resources>